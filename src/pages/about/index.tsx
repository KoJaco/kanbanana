import type { NextPage } from 'next';

// TODO: This should be a completely statically generated page that just explains: 1) what the app is about, 2) who the app is for, 3) a bit about myself, link to github, etc... 4) limitations of the app, 5) potential future road map.

const About: NextPage = () => {
    return <div></div>;
};

export default About;
