import React from 'react';

// TODO: create a checked task, similar styling to Task component but with a checkbox next to it which changes the styling via a done or complete state... Must update database to include this.

const CheckedTask = () => {
    return <div>CheckedTask</div>;
};

export default CheckedTask;
