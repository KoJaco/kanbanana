import React from 'react';
import dynamic from 'next/dynamic';

const DatabaseInfo = () => {
    // TODO: Allow import and export, probably should be a modal with file drop in.... also show board info, another dynamic component.
    return <div>DatabaseInfo</div>;
};

export default DatabaseInfo;
